# Build/check instructions

1. Run `pnpm install` at repo root (workspaces)
2. cd apps/web && pnpm dev or pnpm build
3. cd apps/api - NestJS skeleton

Note: Many pieces are placeholders. Do not run in production.
